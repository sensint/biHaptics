using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AttachCylinder : MonoBehaviour
{
    // Start is called before the first frame update
    [Header("Controller Attachment")]
    public Transform leftEnd;  // Reference to the left hand
    public Transform rightEnd; // Reference to the right hand

    // Update is called once per frame
    void Update()
    {
        
    }
}
